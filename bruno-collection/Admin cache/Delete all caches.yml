info:
  name: Delete all caches
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{domain_url}}/admin/cache/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
