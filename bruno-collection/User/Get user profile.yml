info:
  name: Get user profile
  type: http
  seq: 2

http:
  method: GET
  url: "{{domain_url}}/user/profile/{{user-token}}"
  auth: inherit

runtime:
  variables:
    - name: user-token
      value: e83691b2-0ed4-4c15-b18e-930836b1b121

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
