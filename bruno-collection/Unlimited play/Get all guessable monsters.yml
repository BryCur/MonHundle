info:
  name: Get all guessable monsters
  type: http
  seq: 2

http:
  method: GET
  url: "{{domain_url}}/resources/monster-choices"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
