info:
  name: Create Challenge
  type: http
  seq: 2

http:
  method: POST
  url: "{{domain_url}}/admin/daily/specific-answer"
  body:
    type: json
    data: |-
      {
        "date": "2026-04-23T11:55:01.269Z",
        "monsterId": 62
      }
  auth:
    type: bearer
    token: "{{admin_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
